{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run rubocop",
			"type": "shell",
			"command": "git diff --name-only --diff-filter=MA | xargs rubocop -a",
			"group": "build",
			"presentation": {
				"echo": true,
				"reveal": "always"
			}
		}
	]
}
